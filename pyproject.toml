[tool.poetry]
name = "azuredatalakeintermediate"
version = "0.1.0"
description = "Exploring azure resources deployment using Terraform and service principals principle."
authors = ["arnaud_pesenti <arnaud_pesenti@yahoo.fr>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
azure-identity = "^1.20.0"
azure-keyvault-secrets = "^4.9.0"
azure-storage-file-datalake = "^12.18.1"
python-dotenv = "^1.0.1"
beautifulsoup4 = "^4.13.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
